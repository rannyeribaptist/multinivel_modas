= simple_form_for withdraw, html: { id: "authorize_#{withdraw.id}" } do |f|
  = f.input :status, as: :hidden, input_html: { value: "Autorizado" }

= simple_form_for withdraw, html: { id: "deny_#{withdraw.id}" } do |f|
  = f.input :status, as: :hidden, input_html: { value: "Negado" }
