%h1 Listing users

%table
  %thead
    %tr
      %th Name
      %th Role
      %th Graduation
      %th Level
      %th Avatar
      %th Phone
      %th Social security number
      %th Tax number
      %th
      %th
      %th

  %tbody
    - @users.each do |user|
      %tr
        %td= user.name
        %td= user.role
        %td= user.graduation
        %td= user.level
        %td= user.avatar
        %td= user.phone
        %td= user.social_security_number
        %td= user.tax_number
        %td= link_to 'Show', user
        %td= link_to 'Edit', edit_user_path(user)
        %td= link_to 'Destroy', user, method: :delete, data: { confirm: 'Are you sure?' }

%br

= link_to 'New User', new_user_path
