<%# app/views/shopping_carts/add_item.js.erb %>
<% js = escape_javascript(
  render(partial: 'shopping_carts/list', locals: { shopping_cart: @shopping_cart })
) %>
$("#results").html("<%= js %>");

<% blob = escape_javascript(
  render(partial: 'layouts/shopping_cart')
) %>
$("#cart").html("<%= blob %>");
